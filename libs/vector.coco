data vector2(x, y):
    def __abs__(self) =
        (self.x**2 + self.y**2)**0.5

data vector(*pts):
    def __new__(cls, *pts):
        match [v `isinstance` vector] in pts:
            return v
        else:
            return pts |*> makedata$(cls)
